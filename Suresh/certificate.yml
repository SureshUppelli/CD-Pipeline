apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: node-app-tls
  namespace: default
spec:
  secretName: node-app-tls
  renewBefore: 360h
  dnsNames:
  - testcloud.wetasker.com
  - www.testcloud.wetasker.com
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer